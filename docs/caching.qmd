---
title: "Caching"
---

Every fetch is cached, and the cache is the reason a re-run is cheap.

## What is kept

| Path | What | Size |
| --- | --- | --- |
| `data/raw/oai/` | OAI-PMH XML with listings, keywords and abstracts, by set and page offset | one XML page per OAI-PMH page |
| `data/raw/oai/*.checkpoint.json` | Next OAI-PMH resumption token for an interrupted full run | one tiny JSON file per active set |
| `data/raw/pdf_text/<id>.txt` | First pages of the PDF, as plain text | ~5 KB each |
| `data/raw/pdfs/<id>.pdf` | Only with `--keep-pdf` | megabytes |

`<id>` is the Skemman handle suffix throughout, so `data/raw/pdf_text/25337.txt` and
<https://skemman.is/handle/1946/25337> are the same thesis.

OAI-PMH pages use readable XML filenames such as `oai_dc_com_1946_2064_000000.xml`.
Re-running the same `skemman oai-pmh` command reuses those XML pages rather than waiting
through the request delay again. These pages include `dc:subject` keywords and
`dc:description` abstracts, so the OAI step can preload them before item pages are fetched.

During a full paginated OAI-PMH run, the harvester also writes a checkpoint file such as
`oai_dc_com_1946_2064.checkpoint.json`. It stores the next resumption token only after the
current page's rows have been written to DuckDB, so an interrupted run resumes without
skipping a page. The file is deleted when the set finishes. Runs with `--limit` are treated
as quick checks and do not update the checkpoint.

The standard `oai_dc` pages do not include the file table. DSpace's `xoai` metadata does,
so `files-load` can fill `thesis_file` from cached XML. `files-index` still requests item
pages when Skemman's explicit open/closed labels are needed. It stores only the parsed file
rows in DuckDB, not raw item HTML.

## Why text and not the PDF

A master's thesis averages about 7 MB and the largest seen is over 500 MB. Keeping the
PDFs for one university's master's theses costs around 12 GB; keeping the first eight
pages as text costs about 20 MB.

The text is all the parser reads, so nothing is lost — and the PDF can always be fetched
again from Skemman if you need it.

## The page-count marker

The first line of each text file is a marker holding the PDF's page count:

```
%%PAGES	265
Faculty of Industrial Eng., Mechanical Eng. and Computer Science
University of Iceland
2016
ALICE: Analysis & Learning
Iterative Consecutive Executions
Helga Ingimundardóttir
...
```

That is the real first lines of `data/raw/pdf_text/25337.txt`, from
[thesis 25337](https://skemman.is/handle/1946/25337). Everything the parser reads is
there: the faculty, the school, the year and the title.

Without it, deleting the PDF would throw away how long the thesis was. With it, the number
survives in five bytes.

## Improving the parser costs nothing

Because the text is cached, a better parser can be run over everything already harvested
without a single request:

```bash
duckdb thesis.db -c "drop table thesis_titlepage"
skemman titlepage-load
```

That re-parses at roughly 75 theses a second, against 0.4 a second when fetching.

## What is not cached

A PDF that cannot be read is **not** cached, so a truncated download is retried next time
rather than being silently skipped forever.

A PDF that reads but yields no text — a scan — *is* cached, with its page count. That is a
real answer, not a failure.
